<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>Shopping Cart</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
	    <link rel="stylesheet" href="style/shoppingcart.css" />
	</head>
    <body>
	<div class="content-container">

		{# Cart (right side bar) #}
		<button class="position-fixed top-0 end-0 m-3 p-2 bg-transparent border-0 cart-button"
				data-bs-toggle="offcanvas" 
				data-bs-target="#offcanvasScrolling"
				aria-label="Shopping Cart"
				id="cartButton">
				<img src="style/image/cart4.svg" alt="Cart Icon" 
				class="w-100" 
				style="width: 50px; height: 50px; filter: brightness(0) 
				saturate(100%) invert(9%) sepia(100%) saturate(7488%) 
				hue-rotate(248deg) brightness(96%) contrast(143%);">
		</button>

		<div class="offcanvas offcanvas-end" data-bs-scroll="true" data-bs-backdrop="false" tabindex="-1" id="offcanvasScrolling">
			<div class="offcanvas-header">
				<h5 class="offcanvas-title">Your Cart</h5>
				<button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
			</div>
			<div class="offcanvas-body d-flex flex-column">
				<!-- Cart Items Container -->
            	<div id="cartItemsContainer" class="flex-grow-1 overflow-auto">
					<p class="text-center">Your cart is empty</p>
				</div>
				
				<!-- Spacer pushes button down -->
				<div class="mt-auto"></div>
				
				<!-- Total and Checkout -->
				<div class="text-center py-3 border-top">
					<h5>Total: $<span id="cartTotal">0.00</span></h5>
					<button class="btn btn-primary mt-2">Checkout</button>
            	</div>
			</div>
		</div>
	</div>
    </body>
</html>